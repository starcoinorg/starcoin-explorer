// import * as types from './constants';

const initState = {
};

export default function reducers(state: any = initState, action: any) {
  switch (action.type) {
    default:
      return state;
  }
}